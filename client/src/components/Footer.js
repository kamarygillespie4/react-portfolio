import React from "react";
const styles = {
  position: {
    position: "absolute",
    bottom: "0",
    width: "100%",
    height: "2.5rem",
  },
};

export default function Footer() {
  return <div style={styles.position}>Footer</div>;
}
